---
// Lesson 2.1 — Coins: Triggers, Tags & Your First Stat
// The pivotal lesson. No prefab in this project carries a Rigidbody2D, and only
// one carries any collider at all: Switch.prefab, a trigger BoxCollider2D with no
// body behind it — and Lever.cs (which that prefab uses) has no physics callback
// whatsoever, so that trigger fires into nothing until the student wires it up.
// The student therefore builds the first object they make REACT to the player,
// which means discovering the physics rule and giving the Player a body.
// (An earlier version of this note said nothing in the project had a collider;
// Switch.prefab contradicts it, and that overstatement propagated into 6.4 and
// 8.2 before it was caught.)
// Consequence surfaced deliberately: the moment the Player has a body, the
// Cave Entrance colliders (solid, wired to SceneSwitch.OnCollisionEnter2D)
// start teleporting the student to another scene.
// Stats live on PlayerController. StatManager stays empty until Unit 5.
//
// Scaffolding: FULL (level 1) — this is the first object in the game that
// reacts to anything, so every step is given. The teacher's example for this
// end of the ladder: "we can give them everything on the coin."
//
// The old "Read Function-Level Examples" block (grocery door, keycard
// scanner, rocket stage, baggage carousel, air-hockey table) has been
// removed. Five foreign snippets plus six questions was the shape that
// stranded students in 1.4; the walkthrough replaces it, and the two facts
// those snippets carried (trigger means report-not-push, top-down means
// gravity off) are now `why` lines on the exact steps that need them.
import Lesson from '../../layouts/LessonLayout.astro';
import { renderWalkthrough, renderAskBanner } from '../../lib/walkthrough';
import { renderDecompose } from '../../lib/decompose';

// The two recurring questions, applied to the coin pickup — the first object
// in the game that reacts to anything. Level 1, so every part is given in full.
// The last three parts are the teacher's own split for this lesson: confirm the
// thing that triggered was a coin, change the count, destroy the object. The
// first two are in the list because this is the lesson where the physics rule
// arrives: colliders on both objects and a body on at least one, and until that
// is true the method never runs and the Console says nothing about why. A split
// that covered only the C# would send students hunting a script bug that lives
// in the Inspector.
const DECOMPOSE = {
  big:
    'Make a coin disappear when I touch it — "how do I detect a collision, and make something ' +
    'happen?" As one problem it is not even startable, because at this moment nothing in the ' +
    'whole project is built to notice that two objects touched.',
  parts: [
    {
      name: 'Give the coin a shape the engine can test',
      does: 'A Circle Collider 2D on the prefab root, its radius dragged until the green outline covers the art, and `Is Trigger` ticked. Trigger matters: a solid collider shoves you back, a trigger only reports. Leave the collider on the root, because the root is the object carrying the `Coin` tag.',
      doc: 'the `Collider2D` row in the quick reference, and the Is Trigger checkbox in the Inspector.',
      href: '../docs/reference/unity-csharp-quickref.html',
    },
    {
      name: 'Give the Player a shape and a body',
      does: 'A Circle Collider 2D on the Player root — the root, not the Sprite child, because `CompareTag` is asked of whichever object owns the collider — plus a Rigidbody 2D with Body Type Dynamic, Gravity Scale 0 and Interpolate set. Two colliders with no body anywhere produce silence: no error, no warning, nothing to debug.',
      doc: 'the Body Type warning in the quick reference — it is the first thing to check when nothing happens.',
      href: '../docs/reference/unity-csharp-quickref.html',
    },
    {
      name: 'Confirm the thing that triggered was a coin',
      does: '`OnTriggerEnter2D` runs on the first frame two shapes overlap, and its parameter is the other object\'s collider. The `if (other.CompareTag("Coin"))` check is what turns "something touched me" into "a coin touched me" — spikes, walls and cave doors all land in this same method before the unit is over.',
      doc: 'the `OnTriggerEnter2D(Collider2D other)` and `other.CompareTag("Coin")` rows in the quick reference.',
      href: '../docs/reference/unity-csharp-quickref.html',
    },
    {
      name: 'Change the count',
      does: '`coins = coins + 1;` inside the check, with `coins` declared as a `[SerializeField] private int` back in the class body. No type in front of the line in the method: this assigns to a field that already exists, and declaring it here instead would rebuild it every frame and leave the real total at zero.',
      doc: 'the `[SerializeField] private float speed;` row in the quick reference, and the declare-vs-assign rule you met in 1.3.',
      href: '../docs/reference/unity-csharp-quickref.html',
    },
    {
      name: 'Destroy the object',
      does: '`Destroy(other.gameObject);` — `other` is a Collider2D, not a GameObject, so `.gameObject` walks up to the whole coin. Write `Destroy(gameObject)` by mistake and you delete the Player instead, which is the bug you will deliberately cause in the Bug Hunt.',
      doc: 'the `Destroy(gameObject)` row in the quick reference, and the pickup pattern underneath it.',
      href: '../docs/reference/unity-csharp-quickref.html',
    },
  ],
  why:
    'Three of these five parts are the pickup pattern in miniature — a check, a change, a destroy — ' +
    'and you will write them in that order all year. The first two are what this lesson is really ' +
    'about: Unity\'s rule is a collider on both objects and a body on at least one, and until it ' +
    'is satisfied the method never runs at all. That is why the split has to include the Inspector.',
  sizeTest:
    'The test: when the coin does nothing, is there one part above you can point at and test on ' +
    'its own? A silent failure with no error in the Console is a physics problem, not a script ' +
    'problem — and a split that stops at the C# cannot tell you which.',
  turns: [
    'Part three is the line that turns "something touched me" into "a coin touched me". In one sentence, say what the Player would do on the first frame it walked into a wall if that line were missing.',
  ],
};

const WALK = {
  ask: 'how do I make a coin disappear when I touch it?',
  file: 'Assets/Prefabs/Coin and Assets/Scripts/PlayerController.cs',
  intro: 'Two objects to wire up in the Inspector, then one method on the Player. Every setting and every line is given below.',
  level: 1,
  steps: [
    {
      title: 'Give the Coin a shape',
      lead: 'A coin is currently a picture and nothing else. Start by giving it a shape the physics engine can test.',
      lines: [
        { code: 'Hierarchy or Project ▸ select the Coin prefab', what: 'Work on the **prefab asset** in `Assets/Prefabs`, not one instance in the scene. Anything you change on the prefab applies to every coin you ever place.' },
        { code: 'Coin (root) ▸ Add Component ▸ Circle Collider 2D', what: 'This is the coin\'s **shape**. Before this, the engine has no idea the coin occupies any space at all.' , why: 'A Circle is the right shape here because the coin is round. Unity also offers Box, Capsule, Polygon and Edge — pick the one that matches the silhouette, because a bad shape means a coin you can collect by touching empty air.' },
        { code: 'Circle Collider 2D ▸ Radius   →  drag to fit the coin', what: 'Check the green outline in the Scene view. Does it cover the coin you can actually see?' , why: 'Two things to get right here. The **size**: a new collider does not fit itself to your art, so drag the radius until the green outline covers the coin. The **place**: leave the collider on the root, because the root is the object carrying the `Coin` tag. Move it down to the Untagged Sprite child and `CompareTag("Coin")` becomes false, and the pickup silently does nothing.' },
        { code: 'Circle Collider 2D ▸ Is Trigger   ☑ ON', what: 'Tick it. This changes what an overlap **means**: a solid collider shoves objects apart, a trigger does not push at all — it only **reports** that the overlap happened.' , why: 'Leave this off and the coin behaves like a wall. You still get a message, but you also get stopped dead in your tracks — which is a bug you can feel immediately.' },
      ],
    },
    {
      title: 'Give the Player a shape AND a body',
      lead: 'A collider alone is not enough. Unity\'s rule: colliders on both objects, a Rigidbody2D on at least one.',
      lines: [
        { code: 'Assets/Prefabs/Player (root) ▸ Add Component ▸ Circle Collider 2D', what: 'The Player\'s **shape**. Add it to the **root**, then drag the radius until the green outline fits the character you can see.' , why: '**The root, not the Sprite child**, and this is not a style preference. The cave-entrance script checks `collision.gameObject.CompareTag("Player")`, and `collision.gameObject` is whichever object owns the collider. Put the collider on the Untagged Sprite child and the cave door never opens for you — with no error to explain why.' },
        { code: 'Assets/Prefabs/Player (root) ▸ Add Component ▸ Rigidbody 2D', what: 'The **body**. This puts the Player under simulation, and it is what makes overlap messages fire at all.' , why: '**This is the component everyone forgets.** Two colliders with no body anywhere produce total silence — no error, no warning, no message, nothing to debug. If your coin does nothing at all later, come back to this line first.' },
        { code: 'Rigidbody 2D ▸ Body Type       =  Dynamic', what: 'A Dynamic body is simulated: it collides, and it can be pushed. This is what you want for a character that gets knocked back later.' , why: 'Keep it **Dynamic**. You are still Dynamic at the end of the year, including in the cave where gravity matters. Notice what you just chose: Dynamic is the body type that actually **collides**. Right now nothing in the world is solid to you — in 2.2 you make the spikes solid, and this is the setting that decides whether you stop at them.' },
        { code: 'Rigidbody 2D ▸ Gravity Scale   =  0', what: 'Set it to **0**. Your world is seen from directly above and has no "down" for anything to fall toward.' , why: 'The default is **1**. Leave it and your character immediately starts sliding off the bottom of the screen the moment you press Play. This is the single most common surprise in this lesson.' },
        { code: 'Rigidbody 2D ▸ Interpolate     =  Interpolate', what: 'Smooths the rendered position between physics steps. Physics runs on its own clock, and without this your movement looks slightly jerky.' },
        { code: '( Constraints are still empty for now — see the note below. )', what: 'A Dynamic body picks up spin when something solid hits it off-centre. There is almost nothing solid to you yet, so you leave this alone today and deal with it properly in 2.2.' , why: 'One exception, and you may meet it in this lesson: the cave entrances are solid. If your character tumbles after touching one, that is a Dynamic body picking up torque — not a broken script. You will freeze the rotation in 2.2, next lesson.' },
      ],
    },
    {
      title: 'Write the pickup on the Player',
      lead: 'One new field and one new method on PlayerController. The Player is what moves, so the Player is what gets told about the overlap.',
      lines: [
        { code: '    [SerializeField] private int coins;', what: 'How many coins you have collected. An `int` (whole number), exposed to the Inspector so you can watch it change while the game runs.' , why: 'Declared in the **class body**, not inside the method — the same rule from 1.3. This is state that has to still be there on the next frame, so it belongs to the object.' },
        { code: '', what: '' },
        { code: '    private void OnTriggerEnter2D(Collider2D other)', what: 'Unity calls this on the **first frame** two shapes overlap. You do not call it yourself. The parameter is the *other* object\'s collider — the one that ran into you.' },
        { code: '    {', what: '' },
        { code: '        if (other.CompareTag("Coin"))', what: 'Check **what** you just touched before doing anything. `CompareTag` asks the engine directly; comparing tag strings with `==` builds a new string every call and can silently miss.' , why: 'Spikes, cave entrances and healing pickups will all land in this same method before the unit is over. Without this check, the Player would vanish the first time it walked into a wall that happened to have a collider.' },
        { code: '        {', what: '' },
        { code: '            coins = coins + 1;', what: 'Add one to the running total. Notice there is no type in front — `coins` already exists, so this **assigns** to it. Writing `int coins = coins + 1;` here would create a brand-new local that dies at the end of the frame and leave the real total frozen at zero.' },
        { code: '            Debug.Log("Coins collected: " + coins);', what: 'Print the new total. With no UI in this game yet, the Console is the only way to see the number go up.' },
        { code: '            Destroy(other.gameObject);', what: 'Removes the coin. `other` is a **Collider2D**, not a GameObject — `.gameObject` walks up from the collider to the whole object it belongs to.' , why: '**`other.gameObject` — not `gameObject`.** Using plain `gameObject` destroys the Player, which is the bug you will deliberately cause in the Bug Hunt below. Get it backwards and the game is instantly unplayable.' },
        { code: '        }', what: '' },
        { code: '    }', what: 'Closes the method. The tag check exists so the destroy only ever reaches a coin.' },
      ],
    },
  ],
  finalFile: `// Added to Assets/Scripts/PlayerController.cs, alongside your movement code.
// The fields all sit together in the class body; the method sits below Update().

    [SerializeField] private int coins;

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Coin"))
        {
            coins = coins + 1;
            Debug.Log("Coins collected: " + coins);
            Destroy(other.gameObject);
        }
    }

// Inspector changes made in this lesson:
//   Assets/Prefabs/Coin      : + Circle Collider 2D (Is Trigger ON, radius fitted)
//   Assets/Prefabs/Player    : + Circle Collider 2D on the ROOT (radius fitted)
//                              + Rigidbody 2D  (Dynamic, Gravity Scale 0,
//                                               Interpolate)
//                              Constraints left empty on purpose — 2.2 sets
//                              Freeze Rotation Z once things are solid.`,
  finalNote:
    'Compare against your own file and Inspector. The two things to check hardest: that `coins` is declared in the class body rather than inside the method, and that the destroy says `other.gameObject`.',
};

const body = `<h1>2.1 — Coins: Triggers, Tags &amp; Your First Stat</h1>

<p class="lesson-lead">
    Your character can walk, but it cannot <em>touch</em> anything. Walk straight over the coin
    and nothing happens — not because your code is wrong, but because nothing in this entire
    project is built to notice a collision. Today you make the first object in the game that
    reacts to the player, and in doing so you switch the physics engine on for good.
</p>

${renderAskBanner('how do I detect a collision, and make something happen?', 1)}

${renderDecompose('2.1', DECOMPOSE, 1)}

<!-- ============================================================ -->
<div class="activity discovery">
    <div class="activity-header">
        <span class="activity-label">The Problem</span>
        <span class="activity-time">~10 min</span>
    </div>

    <p>
        <strong>Your goal: make a coin disappear when the player touches it.</strong> Before
        you write a single line, answer this honestly — how would Unity ever <em>know</em> the
        two objects touched? The Player has a Transform. The Coin has a Transform. Two
        positions in space do not collide. Nothing in the engine is checking.
    </p>
    <p>
        Something has to tell it what <em>shape</em> each object is, and something has to be
        simulated every frame so those shapes can be tested against each other. Read what Unity
        gives you:
    </p>

    <table>
        <tr><th>Component</th><th>What it contributes</th></tr>
        <tr>
            <td><code>Collider2D</code></td>
            <td>Gives an object a <strong>shape</strong> the physics engine can test against other shapes. Without one, an object is invisible to physics — it is only a picture.</td>
        </tr>
        <tr>
            <td><code>Rigidbody2D</code></td>
            <td>Puts an object <strong>under simulation</strong>: the engine tracks it every physics step, and it is what makes overlap messages fire at all.</td>
        </tr>
        <tr>
            <td><code>Is Trigger</code> (checkbox on any Collider2D)</td>
            <td>Changes what an overlap <em>means</em>. A solid collider pushes objects apart. A trigger does not push — it only <strong>reports</strong> that the overlap happened.</td>
        </tr>
        <tr>
            <td><code>OnTriggerEnter2D</code></td>
            <td>The message your script receives on the first frame two shapes overlap. This is the event a coin pickup is built on.</td>
        </tr>
        <tr>
            <td><code>CompareTag</code></td>
            <td>How you check <em>what</em> you just touched. Asks the engine whether a collider carries a given tag.</td>
        </tr>
    </table>

    <div class="callout info">
        <strong>The rule that catches everyone.</strong> Unity's 2D physics only simulates
        objects that have a <strong>Collider2D</strong>, and overlap messages only fire when
        <strong>at least one of the two objects also has a Rigidbody2D</strong>. Two colliders
        with no body anywhere produce <em>silence</em> — no error, no warning, nothing to debug.
        <br><br>
        And here is the part that matters today: in this project <strong>nothing in
        <code>Assets/Prefabs</code> has either one — except the Switch</strong>. Not the Player.
        Not the Coin. Not the Spikes. You are about to be the first person to give anything in
        this game a physics body.
    </div>

    <div class="socratic">
        <h3>Socratic: What Is a Collider For?</h3>
        <ol>
<li>A Transform stores position, rotation and scale. What does a Collider2D store that a Transform has no way to express?</li>
<li>The physics engine runs on its own schedule, not inside your <code>Update()</code>. Why would Unity be built that way, instead of letting your script test for overlaps once a frame?</li>
<li>A trigger pushes nothing — it only reports. Name two game behaviours besides a coin pickup where a report-only overlap is the right tool.</li>
<li>A collider is a shape; a rigidbody is a thing under simulation. The Player is the object that moves. Which of the two must live on the Player for a coin to ever notice it, and which one <em>could</em> live on the coin instead?</li>
</ol>
    </div>
</div>

<!-- ============================================================ -->
${renderWalkthrough(WALK)}

<!-- ============================================================ -->
<div class="activity basic-app">
    <div class="activity-header">
        <span class="activity-label">What You Just Switched On</span>
        <span class="activity-time">~10 min</span>
    </div>

    <div class="callout trap">
        <strong>A body changes the whole game — go and see what you broke.</strong>
        The moment the Player has a Collider2D and a Rigidbody2D, <em>every</em> collider in the
        scene starts interacting with it. That includes the <strong>Cave Entrances</strong>,
        which carry solid colliders wired to a scene-switching script that has been sitting
        dormant this whole time, precisely because your Player had no collider to trip it.
        <br><br>
        So: walk into a cave mouth and find out what happens. If you are suddenly standing in a
        different scene, <strong>nothing is broken — you switched the physics engine on.</strong>
        Write down what happened and why. Do <em>not</em> fix it yet, and do not delete the cave
        entrances: Unit 5 builds the cave properly, and this is the first thread of it.
    </div>

    <div class="callout info">
        <strong>One number, one home.</strong> For now the coin count lives on
        <code>PlayerController</code>. There is a <code>StatManager.cs</code> in
        <code>Assets/Scripts</code> — it is empty, and it stays empty. Later in the year you will
        have a real reason to move your stats into a manager, and that reason will come from a
        scene change, not from tidiness. Until then, every stat you build belongs to the player.
    </div>
</div>

<!-- ============================================================ -->
<div class="activity basic-app">
    <div class="activity-header">
        <span class="activity-label">Read the Docs: Where Should a Stat Live?</span>
        <span class="activity-time">~10 min</span>
    </div>

    <p>
        You just made a real design decision without being told to. Here are the three honest
        options for where a number like "coins" can live, and what each one costs:
    </p>

    <table>
        <tr><th>Approach</th><th>What it looks like</th><th>What it costs</th><th>When it wins</th></tr>
        <tr>
            <td><strong>(a) A field on <code>PlayerController</code></strong></td>
            <td><code>[SerializeField] private int coins;</code></td>
            <td>The player owns the number, so anything else that wants it must find the player first.</td>
            <td><strong>Now.</strong> The only thing that touches coins <em>is</em> the player.</td>
        </tr>
        <tr>
            <td><strong>(b) A separate <code>PlayerStats</code> component</strong></td>
            <td>The numbers live in a component whose only job is numbers.</td>
            <td>One more component, one more reference to cache, one more thing to keep in sync.</td>
            <td>When <code>PlayerController</code> grows long enough that movement code and stat code start fighting over the same file.</td>
        </tr>
        <tr>
            <td><strong>(c) A global singleton <code>StatManager</code></strong></td>
            <td>Any script, anywhere, reads the numbers from one shared object.</td>
            <td>Everything can reach it, so nothing protects it — and to be worth having at all, it must survive a scene change.</td>
            <td>When several systems need the same numbers <em>and</em> those numbers must outlive a scene load.</td>
        </tr>
    </table>

    <div class="socratic">
        <h3>Where Does a Number Belong?</h3>
        <ol>
<li>Which of the three did you just build, and which line of your own code proves it?</li>
<li>Open <code>Assets/Scripts/StatManager.cs</code> and read it. If you filled it in as a singleton today, would your coin count survive a scene change? Would that be a feature or a problem <em>right now</em>?</li>
<li>The coin disappears when collected, but "how many coins I have" is not a property of the coin. So where does that number really belong — the object that vanishes, or the object that persists?</li>
</ol>
    </div>
</div>

<!-- ============================================================ -->
<div class="activity basic-app">
    <div class="activity-header">
        <span class="activity-label">Bug Hunt &amp; Tinker</span>
        <span class="activity-time">~25 min</span>
    </div>



    <div class="checklist">
        <h3>Checkpoint — before moving on, you must be able to:</h3>
        <ul>
            <li>Explain what a Collider2D is for, and what a Rigidbody2D adds on top of it</li>
            <li>State the rule — colliders on both, a body on at least one — and say which object you gave the body to, and why</li>
            <li>Explain trigger versus solid, and defend the coin's choice</li>
            <li>Explain what the parameter in <code>OnTriggerEnter2D</code> actually refers to, and why <code>CompareTag</code> beats comparing tag strings</li>
            <li><strong>Point at your own file and say why <code>coins</code> is declared in the class body while <code>coins = coins + 1;</code> is not</strong></li>
            <li>State where the coin count lives, and name one alternative you rejected, with a reason</li>
            <li>Explain what happened at the cave entrance, and why it is not a bug</li>
            <li>Commit and push</li>
        </ul>
    </div>
</div>

<!-- ============================================================ -->
<div class="resources">
    <h3>Reference Docs</h3>
    <p>Need a deeper explanation? These pages cover the same concepts:</p>
    <ul>
        <li><a href="../docs/unit-02/index.html">Unit 2: Stats &amp; Pickups</a> — colliders, triggers, and health in depth</li>
        <li><a href="../docs/unit-02/triggers-vs-collisions.html">Triggers vs Collisions</a> — the complete reference for this physics family, used all year</li>
        <li><a href="../docs/reference/unity-csharp-quickref.html">Unity + C# Quick Reference</a> — Collider2D and Rigidbody2D APIs</li>
        <li><a href="../docs/ask.html">How do I…? index</a> — find this lesson again by the question you were asking</li>
    </ul>
    <p>Next: <a href="2.2.html">2.2 — Spikes: Physics, Damage &amp; Death</a>, where you discover that the movement you wrote ignores all of this.</p>
</div>`;
---
<Lesson id="2.1" title="2.1 Coins: Triggers, Tags &amp; Your First Stat — AP CS Principles" root=".." css="../style.css">
  <Fragment set:html={body} />
</Lesson>
